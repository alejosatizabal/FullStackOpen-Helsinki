{
  "//": [
    "COMENTARIOS, DEBEN ESTAR EN LA RAÍZ",
    "A sample express.js project",
    "Uses nodemon for development",
    "Issue npm run dev to start your development server"
  ],
  "name": "apr-part3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "//start": "echo 'Comentario: Agregado despues de _npm init_'",
    "dev": "nodemon index.js",
    "//dev": "echo 'Comentario: Agregado despues de instalar _npm nodemon_'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "//ORIGINALbuild:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend",
    "build:ui": "rm -rf dist && cd ../apr-part2/ && npm run build && cp -r dist ../apr-part3/",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "scripts-comments": {
    "start": "Comentario: Agregado despues de 'npm init'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
